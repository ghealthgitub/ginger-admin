services:
  - type: web
    name: ginger-admin
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    disk:
      name: ginger-uploads
      mountPath: /opt/render/project/src/uploads
      sizeGB: 1
